import React from 'react';

const NoResults = props => (
    <li className='not-found'>
        <h3>Sorry, no images match your search.</h3>
    </li>
);                                                                              // creates a no result tab

export default NoResults;
